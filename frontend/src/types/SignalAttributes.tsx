import { IconDefinition } from "@fortawesome/fontawesome-svg-core";

export interface SignalAttributes {
    in_animation: string;
    out_animation: string;
    duration: number;
    baseColor: string;
    backgroundColor: string;
    icon: IconDefinition
}